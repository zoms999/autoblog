# 자동 블로그 포스팅 프로그램 설치 및 설정 가이드

이 문서는 자동 블로그 포스팅 프로그램의 설치 및 설정 방법을 안내합니다.

## 1. 설치 방법

### 1.1 필수 요구사항

- Python 3.7 이상
- pip (Python 패키지 관리자)
- Google 계정 및 Blogger 블로그

### 1.2 프로그램 설치

1. 저장소 클론 또는 다운로드:
   ```
   git clone [repository_url]
   cd 자동블로그포스팅
   ```

2. 필요한 패키지 설치:
   ```
   pip install -r requirements.txt
   ```

## 2. Google API 설정

### 2.1 Google Cloud Console에서 프로젝트 생성

1. [Google Cloud Console](https://console.cloud.google.com/)에 접속합니다.
2. 새 프로젝트를 생성합니다.
3. 왼쪽 메뉴에서 "APIs 및 서비스" > "라이브러리"로 이동합니다.
4. "Blogger API v3"를 검색하여 활성화합니다.

### 2.2 OAuth 클라이언트 ID 생성

1. "APIs 및 서비스" > "사용자 인증 정보"로 이동합니다.
2. "사용자 인증 정보 만들기" > "OAuth 클라이언트 ID"를 선택합니다.
3. 애플리케이션 유형으로 "데스크톱 앱"을 선택합니다.
4. 이름을 입력하고 "만들기"를 클릭합니다.
5. "JSON 다운로드" 버튼을 클릭하여 클라이언트 시크릿 파일을 다운로드합니다.
6. 다운로드한 JSON 파일을 프로젝트 루트 디렉토리에 `client_secret.json`으로 저장합니다.

### 2.3 OAuth 동의 화면 설정

1. "OAuth 동의 화면" 탭으로 이동합니다.
2. 사용자 유형으로 "외부"를 선택합니다.
3. 앱 이름, 사용자 지원 이메일 등 필요한 정보를 입력합니다.
4. 범위 추가 단계에서 "blogger.googleapis.com" 범위를 추가합니다.
5. 테스트 사용자에 본인의 이메일 주소를 추가합니다.

## 3. 환경 변수 설정

1. `.env.example` 파일을 `.env` 파일로 복사합니다:
   ```
   cp .env.example .env
   ```

2. `.env` 파일을 열고 다음 내용을 설정합니다:
   ```
   CLIENT_SECRET_FILE=client_secret.json
   BLOG_ID=your_blog_id   # 처음 실행 시 자동으로 가져올 수 있습니다
   LOG_LEVEL=INFO
   ```

## 4. 프로그램 실행

1. 프로그램 실행:
   ```
   python main.py
   ```

2. 첫 실행 시 Google 계정 인증이 필요합니다:
   - 브라우저가 열리고 Google 로그인 화면이 표시됩니다.
   - 로그인하고 애플리케이션 접근 권한을 허용합니다.
   - 인증이 완료되면 토큰이 `token.json` 파일에 저장됩니다.

3. 프로그램 사용:
   - 스크랩할 URL을 입력합니다.
   - 앱 정보를 입력합니다 (앱 이름, 버전 등).
   - 블로그 카테고리를 입력합니다.
   - 스크랩된 내용을 확인하고 포스팅 여부를 결정합니다.

## 5. 문제 해결

### 5.1 인증 오류
- `client_secret.json` 파일이 올바른 위치에 있는지 확인합니다.
- `.env` 파일의 `CLIENT_SECRET_FILE` 경로가 올바른지 확인합니다.
- 인증 토큰을 재설정하려면 `token.json` 파일을 삭제하고 다시 실행합니다.

### 5.2 블로그 ID 오류
- `.env` 파일에 올바른 `BLOG_ID`가 설정되어 있는지 확인합니다.
- 사용자에게 블로그 관리 권한이 있는지 확인합니다.

### 5.3 스크래핑 오류
- 웹사이트의 로봇 배제 표준(robots.txt) 설정을 확인합니다.
- 웹사이트 구조가 변경되었을 수 있습니다.
- 모든 이미지가 다운로드되지 않을 수 있습니다 (접근 제한 이미지 등).

## 6. 추가 정보

- 일부 웹사이트는 스크래핑을 제한하거나 차단할 수 있습니다.
- 항상 저작권 및 사용 약관을 존중하여 컨텐츠를 사용하세요.
- 대용량의 트래픽을 발생시키는 스크래핑은 웹사이트에 부하를 줄 수 있습니다. 