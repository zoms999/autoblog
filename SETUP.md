# 자동 블로그 포스팅 프로그램 설치 및 설정 가이드

이 문서는 자동 블로그 포스팅 프로그램의 상세 설치 및 설정 방법을 안내합니다.

## 1. 설치 방법

### 1.1 필수 요구사항

- Python 3.7 이상
- pip (Python 패키지 관리자)
- Google 계정 및 Blogger 블로그
- (선택) Google AI Studio 계정 (AI 재작성 기능을 위한 Gemini API 키)

### 1.2 프로그램 설치

1. 저장소 클론:
   ```
   git clone https://github.com/zoms999/autoblog.git
   cd autoblog
   ```

2. 필요한 패키지 설치:
   ```
   pip install -r requirements.txt
   ```

3. 환경 변수 파일 생성:
   `.env` 파일을 프로젝트 루트 디렉토리에 생성하고 다음 내용을 추가합니다:
   ```
   # 플라스크 설정
   SECRET_KEY=your_secret_key_here
   
   # Google API 인증 정보
   CLIENT_ID=your_client_id_here
   CLIENT_SECRET=your_client_secret_here
   
   # 블로그 설정
   BLOG_ID=your_blog_id_here
   
   # Google AI (Gemini) API 설정
   GOOGLE_AI_API_KEY=your_google_ai_api_key_here
   ```

## 2. Google Blogger API 설정

### 2.1 Google Cloud Console에서 프로젝트 생성

1. [Google Cloud Console](https://console.cloud.google.com/)에 접속합니다.
2. 새 프로젝트를 생성합니다 (또는 기존 프로젝트 선택).
3. 왼쪽 메뉴에서 "API 및 서비스" > "라이브러리"로 이동합니다.
4. "Blogger API v3"를 검색하여 활성화합니다.

### 2.2 OAuth 클라이언트 ID 설정

1. "API 및 서비스" > "사용자 인증 정보"로 이동합니다.
2. "사용자 인증 정보 만들기" > "OAuth 클라이언트 ID"를 선택합니다.
3. 애플리케이션 유형으로 "웹 애플리케이션"을 선택합니다.
4. 이름을 입력합니다.
5. "승인된 리디렉션 URI"에 다음을 추가합니다:
   ```
   http://localhost:5000/oauth2callback
   ```
6. "만들기"를 클릭합니다.
7. 생성된 클라이언트 ID와 시크릿을 복사하여 `.env` 파일에 추가합니다.
8. "JSON 다운로드" 버튼을 클릭하여 클라이언트 시크릿 파일을 다운로드합니다.
9. 다운로드한 JSON 파일을 프로젝트 루트 디렉토리에 `client_secret.json`으로 저장합니다.

### 2.3 OAuth 동의 화면 설정

1. "OAuth 동의 화면" 탭으로 이동합니다.
2. 사용자 유형으로 "외부"를 선택합니다 (또는 테스트 목적으로 "내부"를 선택).
3. 앱 이름, 사용자 지원 이메일 등 필요한 정보를 입력합니다.
4. 범위 추가 단계에서 다음 범위를 추가합니다:
   - `https://www.googleapis.com/auth/blogger`
5. 테스트 사용자에 본인의 이메일 주소를 추가합니다.

### 2.4 블로그 ID 확인

1. 구글 블로그 관리 페이지(`https://www.blogger.com/blog/posts/[BLOG_ID]`)에서 URL에 포함된 BLOG_ID를 확인합니다.
2. 또는 Blogger API의 블로그 목록 조회를 통해 확인할 수 있습니다.
3. 확인한 블로그 ID를 `.env` 파일의 `BLOG_ID` 변수에 입력합니다.

## 3. Google AI API 설정 (선택)

AI 재작성 기능을 사용하려면 Google Gemini API 키가 필요합니다:

1. [Google AI Studio](https://aistudio.google.com/) 웹사이트에 접속합니다.
2. Google 계정으로 로그인합니다.
3. API 키 생성:
   - API 키 관리 페이지로 이동합니다.
   - "API 키 생성" 버튼을 클릭합니다.
   - 생성된 API 키를 복사합니다.
4. 복사한 API 키를 `.env` 파일의 `GOOGLE_AI_API_KEY` 변수에 입력합니다.

## 4. Flask 웹 애플리케이션 실행

1. 프로그램 실행:
   ```
   python app.py
   ```

2. 웹 브라우저에서 다음 주소로 접속합니다:
   ```
   http://localhost:5000
   ```

3. 첫 실행 시 Google 계정 인증이 필요합니다:
   - "블로그에 포스팅" 기능을 사용할 때 Google 로그인 화면이 표시됩니다.
   - 로그인하고 애플리케이션 접근 권한을 허용합니다.
   - 인증이 완료되면 토큰이 `token.json` 파일에 저장됩니다.

## 5. 기능 사용 방법

### 5.1 웹 페이지 스크래핑

1. 메인 페이지에서 스크랩할 URL을 입력합니다.
2. 앱 정보를 입력합니다 (앱 이름, 버전 등).
3. 블로그 카테고리를 입력합니다 (쉼표로 구분).
4. "스크랩 시작" 버튼을 클릭합니다.
5. 스크랩이 완료되면 미리보기가 표시됩니다.

### 5.2 AI 컨텐츠 재작성 (Gemini API 설정 필요)

1. 미리보기 페이지에서 "AI로 컨텐츠 재작성" 버튼을 클릭합니다.
2. AI가 컨텐츠를 재작성하고 결과를 표시합니다.
3. 재작성된 컨텐츠가 마음에 들지 않으면 "원본으로 복원" 버튼을 클릭할 수 있습니다.

### 5.3 블로그 포스팅

1. 미리보기 페이지에서 "블로그에 포스팅" 버튼을 클릭합니다.
2. 첫 포스팅 시 Google 인증 과정이 진행됩니다.
3. 포스팅이 완료되면 게시글 링크가 표시됩니다.

## 6. 문제 해결

### 6.1 인증 오류

- `client_secret.json` 파일이 올바른 위치에 있는지 확인합니다.
- OAuth 동의 화면에서 필요한 범위를 모두 추가했는지 확인합니다.
- 리디렉션 URI가 올바르게 설정되었는지 확인합니다.
- 인증 토큰을 재설정하려면 `token.json` 파일을 삭제하고 다시 실행합니다.

### 6.2 Gemini API 오류

- `.env` 파일에 올바른 `GOOGLE_AI_API_KEY`가 설정되어 있는지 확인합니다.
- API 키의 할당량을 초과하지 않았는지 확인합니다.
- API 키가 활성 상태인지 확인합니다.

### 6.3 스크래핑 오류

- 웹사이트의 로봇 배제 표준(robots.txt) 설정을 확인합니다.
- 웹사이트 구조가 변경되었을 수 있습니다.
- 일부 동적 컨텐츠는 스크래핑이 어려울 수 있습니다.

## 7. 프로덕션 배포 고려사항

실제 프로덕션 환경에서 운영할 때는 다음 사항을 고려하세요:

1. 보안:
   - 강력한 `SECRET_KEY`를 설정하세요.
   - `.env` 파일은 공개 저장소에 업로드하지 마세요.

2. 웹 서버:
   - Gunicorn, uWSGI 등의 WSGI 서버를 사용하세요.
   - Nginx와 같은 웹 서버를 프록시로 사용하세요.

3. 데이터베이스:
   - 세션 데이터를 데이터베이스에 저장하는 것을 고려하세요.
   - Redis나 MySQL을 활용하세요.

4. 모니터링:
   - 애플리케이션 로그를 모니터링하세요.
   - 오류 추적 시스템을 구현하세요.

## 8. 추가 정보

- 일부 웹사이트는 스크래핑을 제한하거나 차단할 수 있습니다.
- 항상 저작권 및 사용 약관을 존중하여 컨텐츠를 사용하세요.
- AI 재작성을 통해 컨텐츠를 변경하더라도 출처를 명시하는 것이 좋습니다. 